# Outbox — 2026-05-19 12:xx ICT

## t_ebf10c14 — VERIFIED
**Task:** AgentCard mini-timeline → real /api/agent/:id/timeline data
**Worker:** shihao
**Status:** VERIFIED by anmaioyi

### Fix summary
- `AgentCard.tsx` — removed mockDots hardcode (was at line 30)
- Added `useEffect` that fetches `/api/agent/${agent.id}/timeline?limit=8` on mount
- API returns newest-first → reversed to chronological (oldest leftmost, left-to-right)
- Color mapping by event type:
  - `token_usage` → `bg-blue-400`
  - `session_start` / `session_end` → `bg-green-400`
  - `kanban_event` → `bg-amber-400`
  - default → `bg-slate-400`
- Transparent `bg-transparent` pad dots fill remaining slots to 8
- Dot labels exposed via `title` tooltip attribute

### Verification
- Code matches spec fully
- `stopPropagation` preserved on alert/kill/history buttons
- `onClick` handleCardClick (`openDetailPanel`) intact at line 87
- TypeScript: `TimelineEntry` interface defined, no type errors
- No server/StateEngine changes
- Frontend only

### Acceptance checklist
1. ✅ mockDots hardcode removed
2. ✅ `/api/agent/:id/timeline?limit=8` fetch wired (DEV base: `http://localhost:7878`)
3. ✅ Color mapping by event type (blue/green/amber/slate)
4. ✅ Chronological order — oldest left, newest right
5. ✅ Transparent pad dots to fill 8 slots
6. ✅ `stopPropagation` + `onClick` + history button untouched
7. ✅ typecheck: no new AgentCard errors

**Verdict:** PASS. Ready for /goal Phase 1 backlog.