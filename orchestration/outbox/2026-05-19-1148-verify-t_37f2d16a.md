# Outbox — 2026-05-19 11:48 ICT

## t_37f2d16a — VERIFIED
**Task:** Fix History popup/panel overflow — UI exceeds container bounds
**Worker:** shihao
**Status:** VERIFIED by anmaioyi

### Fix summary
- `StatChart.tsx:145` — tooltip div: added `max-w-[200px]` to cap width so large `toLocaleString()` numbers cannot stretch the tooltip beyond bounds
- `AgentCard.tsx:191-195` — wrapped `<StatChart>` in `<div className="mt-2 overflow-hidden rounded-lg">` so the SVG's `overflow: visible` is clipped at the card boundary

### Verification
- Code matches spec (overflow containment)
- 2 files changed, surgical fix
- No server/StateEngine changes
- Frontend only

### Acceptance checklist
1. ✅ Tooltip has max-width cap
2. ✅ History panel clipped by overflow-hidden wrapper
3. ✅ No AgentCard structural changes beyond overflow wrapper
4. ✅ No server/StateEngine changes

**Verdict:** PASS. Ready for /goal Phase 1 backlog.